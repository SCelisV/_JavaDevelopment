/*
 * DataStructureDecision
 */
package Collections;

public class TreeSet extends Decision {  // TreeSet is a subtype of Decision

    @Override
    // toString
    // This object (which is already a string!) is itself returned.
    // Returns: the string itself.
    public String toString(){
        return ("usando el método toString from class TreeSet");
    }
}
